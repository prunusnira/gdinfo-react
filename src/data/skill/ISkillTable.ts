export interface ISkillTable {
    token: string | null;
    user: string;       // JSON string
    order: string;
    gtype: string;
    ptype: string;
    page: string;
    pages: string;
    lv: string;
    rank: string;
    ver: string;
    hot: string;
    hskill: string;     // JSON string
    oskill: string;     // JSON string
    skill: string;      // JSON string
}