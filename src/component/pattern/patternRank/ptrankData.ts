import React from "react"

type PTRankData = {
    rate: number,
    skill: string,
    ratecolor: React.CSSProperties,
    skillcolor: React.CSSProperties,
    index: number,
    towertitle: string,
    profile: string,
    name: string,
    rank: string,
    fc: boolean,
    exc: boolean,
}

export default PTRankData